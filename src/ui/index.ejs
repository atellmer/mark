<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<meta http-equiv="X-UA-Compatible" content="ie=edge">
	<base href="<%= htmlWebpackPlugin.options.baseURL %>" />
	<link rel="icon" href="/" />
	<meta name="theme-color" content="#333" />
	<% for (var css in htmlWebpackPlugin.files.css) { %><link rel="preload" href="<%= htmlWebpackPlugin.files.css[css] %>" as="style"><% } %>
	<link rel="preload" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap&subset=cyrillic-ext" as="style">
	<style>
		*,
		*::before,
		*::after {
			box-sizing: border-box;
		}
		html {
			font-size: 14px;
			line-height: 1.2;
		}
		body, html {
			margin: 0;
			width: 100%;
			height: 100%;
		}
		body {
			font-family: arial;
			background-color: #F5F8FA;
			color: #262626;
		}
	</style>
	<title><%= htmlWebpackPlugin.options.title %></title>
</head>
<body>
	<div id="app">
		<div>Loading...</div>
	</div>
	<% for (var chunk in htmlWebpackPlugin.files.chunks) { %><script defer src="<%= htmlWebpackPlugin.files.chunks[chunk].entry %>"></script><% } %>
	<% for (var css in htmlWebpackPlugin.files.css) { %><link rel="stylesheet" href="<%= htmlWebpackPlugin.files.css[css] %>"><% } %>
	<link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto:300,400,500,700&display=swap&subset=cyrillic-ext">
</body>
</html>
